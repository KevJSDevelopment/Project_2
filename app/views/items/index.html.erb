<h1> Products </h1>



<ul>
      <% @categories.each do |category|%>
        <h3> <%= category.name.titleize %></h3>
          <% @items.each do |item|%>
            <li><%= link_to item.name, item_path(item.id) %></li>
        <%end%>
    <%end%>
</ul>
